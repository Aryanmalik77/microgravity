# User Profile

*Learned preferences and value systems.*

---

- The user has a non-negotiable preference for the AntiGravity UI Agent Engine.
- Never suggest or use custom GUI scripts or standalone vision scripts.
- Only the ui_planner and ui_executor tools are sanctioned for UI work.

The user has an explicit negative preference: Never use the `browser` tool for tasks that can be accomplished with `ui_planner` and `ui_executor`.

The user has explicitly stated 'The legacy browser tool has been disabled,' which further solidifies their negative preference for the `browser` tool when `ui_planner` and `ui_executor` can accomplish the task. This reinforces the existing constraint: 'Never use the `browser` tool for tasks that can be accomplished with `ui_planner` and `ui_executor`.'

The user is responsive to troubleshooting steps and is committed to enabling the swarm to perform tasks, specifically those involving the UI agent.

The user has an explicit preference for 'Sign in with Google' as an authentication method when performing UI agent tests, and a negative preference against using direct username/password input for such tests. **However, the user has explicitly requested to override this preference for specific tasks when they deem it necessary, allowing for direct username/password input when explicitly instructed.**

The user prioritizes control over the swarm's operational state, wanting to explicitly stop tasks, which implies a desire for a clean slate or to prevent unintended background operations.

The user values the ability to modify or override even 'non-negotiable' rules when they deem it necessary to proceed with a specific task or test alternative methods. This highlights a desire for ultimate control and flexibility in directing the swarm's behavior.

The user values the swarm's ability to interpret and apply updated profile rules in real-time, especially when those updates grant overrides to previously 'non-negotiable' preferences. They expect the swarm to adapt its behavior promptly based on direct instructions.

The user values the swarm's ability to interpret and apply updated profile rules in real-time, especially when those updates grant overrides to previously 'non-negotiable' preferences. They expect the swarm to adapt its behavior promptly based on direct instructions. The user prioritizes exercising explicit control to modify or override even 'non-negotiable' rules when they deem it necessary for specific tasks or to test alternative methods.
